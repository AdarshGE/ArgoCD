# Default values for eventuate-cdc-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: 864787282397.dkr.ecr.us-east-1.amazonaws.com/eventuate-tram-cdc-mysql-service
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "0.21.3.RELEASE-alpine"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: "eventuate-cdc-service"

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

deleteDeployment:
  enabled: false
 
database1:
  site:
    username: sa
    password: Gei654321itc
    name: SOADB
  site1:
    username: sa1
    password: 1Gei654321itc
    name: SOADB1

EvantuateCDC:
  - site: 
    sitedetails:
      SITEID: Site001
      SITEID1: Site002

database:
  - siteID: Site1
    SiteDetails:
      - port1: ""
      - username: sa
      - password: Gei654321itc
      - name: SOADB
      - server: pa-adi1309-int.plantapps-dev.com
      - instance: "1"      
  - siteID: Site2 
    SiteDetails:
      - port1: ""
      - username: sa1
      - password: Gei654321itc1
      - name: SOADB1
      - server: 1pa-adi1309-int.plantapps-dev.com
      - instance: "1"

apiVersion: v1
kind: ConfigMap
metadata:
  name: patch-cdc
data:
  service_entrypoint.sh : sample